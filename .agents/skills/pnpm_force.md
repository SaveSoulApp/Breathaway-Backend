---
name: use-pnpm
description: Forces the agent to use pnpm for all dependency operations instead of npm.
---

Always use `pnpm` for initializing, installing, or running scripts. 
Example: Run `pnpm install` instead of `npm install`.
